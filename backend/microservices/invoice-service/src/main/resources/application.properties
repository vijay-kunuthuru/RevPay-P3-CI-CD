spring.application.name=invoice-service

# --- DYNAMIC DOCKER NETWORKING ---
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/revpay_invoice_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Vijay@2003

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8084

# --- DYNAMIC EUREKA NETWORKING ---
eureka.client.service-url.defaultZone=http://${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka/

management.endpoints.web.exposure.include=*